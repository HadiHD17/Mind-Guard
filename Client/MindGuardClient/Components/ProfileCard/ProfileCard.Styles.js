import { StyleSheet } from "react-native";

export default StyleSheet.create({
  card: {
    width: 350,
    minHeight: 82,
    backgroundColor: "#fff",
    borderColor: "#ddd",
    borderWidth: 1,
    borderRadius: 10,
    padding: 12,
    marginVertical: 10,
    flexDirection: "row",
    justifyContent: "space-between",
    alignItems: "center",
  },
});
